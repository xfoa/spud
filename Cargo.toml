[package]
name = "spud"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"

[dependencies]
iced = { version = "0.14", default-features = true, features = ["image"] }
hostname = "0.4"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
